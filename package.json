{
  "name": "dcp-worker",
  "version": "1.0.11",
  "description": "JavaScript portion of DCP Workers for NodeJS",
  "main": "index.js",
  "bin": {
    "dcp-worker": "dcp-worker"
  },
  "scripts": {
    "postpublish": "PACKAGE_VERSION=$(cat package.json | grep \\\"version\\\" | head -1 | awk -F: '{ print $2 }' | sed 's/[\",]//g' | tr -d '[[:space:]]') && git tag -f v$PACKAGE_VERSION && git push --tags",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kings Distributed Systems",
  "license": "MIT",
  "dependencies": {
    "blessed": "^0.1.81",
    "blessed-contrib": "^4.8.20",
    "chalk": "^4.1.0",
    "dcp-client": "^3.1.13"
  },
  "directories": {
    "lib": "lib"
  },
  "devDependencies": {},
  "repository": {
    "type": "git",
    "url": "git@github.com:Distributed-Compute-Labs/dcp-worker.git"
  },
  "keywords": [
    "dcp"
  ],
  "bugs": {
    "url": "https://github.com/Distributed-Compute-Labs/dcp-worker/issues"
  },
  "homepage": "https://gitlab.com/Distributed-Compute-Protocol/dcp-worker#readme"
}
